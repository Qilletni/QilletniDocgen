mavenCentralPublishing=true
signAllPublications=true

# Single source of truth for the `qilletni-docgen` version. See
# `.qilletni/release.yml` and `RELEASE.md` (in `Qilletni/Qilletni`) for the
# release process.
qilletniDocgenVersion=1.0.1-SNAPSHOT

# Aligned version of the `dev.qilletni.impl:qilletni` and
# `dev.qilletni.api:qilletni-api` dependency, which are always released
# together at the same version (see `qilletni-core` in `release/components.yml`
# of `Qilletni/Qilletni`). Kept as a single property so both coordinates are
# bumped together, never independently.
qilletniCoreVersion=1.0.2
